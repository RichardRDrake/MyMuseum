Use Inno Setup Compiler

Load Installer_Script

Set MyAppVersion to the current version of your build
Confirm paths to the Build and Make sure the Build folder contains "Register.bat"
Change the OutputDir to where you want the Setup file to be located

Compile the script


The installer will install the program and run Register.bat in the install location to regiser the program. It can then be launched from the website